# SHOW FUNCTIONS

## 概述

`SHOW FUNCTIONS` 语句用于列出当前数据库中定义的所有函数，包括内置函数和用户自定义函数。

## 语法

```sql
SHOW FUNCTIONS
```



## 示例

示例 1：列出所有函数

```sql
SHOW FUNCTIONS;
```

执行上述语句后，将返回当前数据库中定义的所有函数的列表，包括内置函数和用户自定义函数。这些函数可能涵盖字符串处理、日期时间操作、数学计算等多个方面。


