## 功能

`SHOW USERS` 命令用于列出当前数据库空间下的所有用户信息。通过执行该命令，用户可以查看和管理数据库中的用户权限和访问控制。

## 语法

```SQL
SHOW USERS;
```

## 示例

```SQL
SHOW USERS;
```

执行该命令后，将返回当前数据库空间下的所有用户列表，包括用户名、权限等级等信息。

## 使用场景

 当需要了解当前数据库空间下的用户情况时，可以使用 `SHOW USERS` 命令快速查看。





