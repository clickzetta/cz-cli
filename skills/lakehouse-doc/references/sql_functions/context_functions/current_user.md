### CURRENT_USER 函数

#### 简介

`CURRENT_USER` 函数用于获取当前数据库会话的用户名称。该函数不需要任何参数，并返回一个字符串类型的值，表示当前会话用户的名称。

#### 语法

```sql
CURRENT_USER()
```

#### 返回结果

`CURRENT_USER` 函数返回一个字符串，表示当前数据库会话的用户名称。

#### 使用示例

1. 查询当前会话的用户名称：

```sql
SELECT CURRENT_USER();
```

   执行该查询后，结果可能如下：

   ```
   user1
   ```
