name: release
run-name: Release ${{ github.event_name == 'push' && github.ref_name || format('v{0}', inputs.version || 'manual') }}

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. 0.1.0)"
        required: false
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          else
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
            echo "tag=v${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ steps.version.outputs.tag }}" \
            --title "${{ steps.version.outputs.tag }}" \
            --generate-notes \
            --repo "${{ github.repository }}" || true

  build:
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            runner: ubuntu-latest
          - os: linux
            runner: ubuntu-24.04-arm
          - os: darwin
            runner: macos-latest
          - os: darwin
            runner: macos-15-intel
    runs-on: ${{ matrix.runner }}
    env:
      GH_REPO: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v4

      - name: Sync clickzetta-skills
        shell: bash
        run: |
          set -x
          WORK_DIR="$(pwd)"
          SKILLS_SRC="$(mktemp -d)/clickzetta-skills"
          curl -sSL --retry 3 --retry-delay 5 --max-time 60 https://github.com/clickzetta/clickzetta-skills/archive/refs/heads/main.tar.gz | tar -xz -C "$(dirname "$SKILLS_SRC")"
          mv "$(dirname "$SKILLS_SRC")/clickzetta-skills-main" "$SKILLS_SRC"
          mkdir -p "$WORK_DIR/skills/external"
          count=0
          for name in $(ls "$SKILLS_SRC" | grep '^clickzetta-'); do
            if [ -f "$SKILLS_SRC/$name/SKILL.md" ]; then
              cp -r "$SKILLS_SRC/$name" "$WORK_DIR/skills/external/$name"
              count=$((count + 1))
            fi
          done
          set +x
          echo "Synced $count skills from clickzetta-skills repo"

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.11"

      - name: Install dependencies
        timeout-minutes: 30
        env:
          HUSKY: "0"
        run: bun install --frozen-lockfile

      - name: Build and upload ${{ matrix.os }} targets
        env:
          OPENCODE_VERSION: ${{ needs.create-release.outputs.version }}
          OPENCODE_RELEASE: "1"
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CLICKZETTA_OTEL_ENDPOINT: ${{ secrets.CLICKZETTA_OTEL_ENDPOINT }}
          CLICKZETTA_OTEL_HEADERS: ${{ secrets.CLICKZETTA_OTEL_HEADERS }}
        run: cd packages/opencode && bun run script/build.ts

  upload-installer:
    needs: [create-release, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Upload install script
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ needs.create-release.outputs.tag }}" \
            scripts/install.sh \
            --clobber --repo "${{ github.repository }}"

  publish-npm:
    needs: [create-release, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Download release binaries
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ needs.create-release.outputs.version }}
          TAG: ${{ needs.create-release.outputs.tag }}
        run: |
          mkdir -p artifacts
          for platform in darwin-arm64 darwin-x64 linux-arm64 linux-x64; do
            mkdir -p "artifacts/cz-cli-${platform}/bin"
            if [ "$platform" = "linux-arm64" ] || [ "$platform" = "linux-x64" ]; then
              gh release download "$TAG" \
                --pattern "cz-cli-${platform}.tar.gz" \
                --dir artifacts --repo "${{ github.repository }}"
              tar -xzf "artifacts/cz-cli-${platform}.tar.gz" -C "artifacts/cz-cli-${platform}"
              mv "artifacts/cz-cli-${platform}/cz-cli" "artifacts/cz-cli-${platform}/bin/cz-cli"
            else
              gh release download "$TAG" \
                --pattern "cz-cli-${platform}.zip" \
                --dir artifacts --repo "${{ github.repository }}"
              unzip -o "artifacts/cz-cli-${platform}.zip" -d "artifacts/cz-cli-${platform}"
              mv "artifacts/cz-cli-${platform}/cz-cli" "artifacts/cz-cli-${platform}/bin/cz-cli"
            fi
          done

      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          scripts/npm-publish.sh "${{ needs.create-release.outputs.version }}" artifacts
